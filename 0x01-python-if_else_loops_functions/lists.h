#ifndef LISTS_H
#define LISTS_H
#include <stdlib.h>
#include <string.h>
#include <stdio.h>
#include "lists.h"
listint_t *insert_node(listint_t **head, int number);
def islower(c):
def uppercase(str):
def print_last_digit(number):
def add(a, b):
def pow(a, b):
def fizzbuzz():

#endif
